namespace BizQuits.Models;

public enum BugSeverity
{
    Low = 0,
    Medium = 1,
    High = 2,
    Critical = 3
}

public enum BugStatus
{
    Open = 0,
    InProgress = 1,
    Resolved = 2
}

public enum BugPriority
{
    Low = 0,
    Normal = 1,
    High = 2,
    Urgent = 3
}
